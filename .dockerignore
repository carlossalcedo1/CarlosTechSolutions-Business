# backend/Dockerfile only COPYs a few specific paths, but the build context
# is the whole repo root — without this, Docker ships node_modules and the
# rest of the frontend source to the daemon on every build for no reason.
frontend/node_modules
frontend/dist
frontend/src
!frontend/src/data/items.json
.git
backend/data
backend/.env
**/__pycache__
